import React from 'react';
import { CompleteProfileForm } from './components/complete-profile-form';

export default function CompleteProfile() {
  return (
    <div className='flex min-h-screen items-center justify-center bg-gradient-to-b from-purple-200 to-white'>
      <CompleteProfileForm />
    </div>
  );
}
